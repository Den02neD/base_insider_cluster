# Base — Insider Cluster Detector

Catches wallets that create **10+ new pools in hours** — factory farmers, launch groups, or coordinated insiders.

One wallet launching dozens of tokens = pattern.

## Run

```bash
python base_insider_cluster.py
